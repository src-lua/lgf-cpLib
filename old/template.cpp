#include <bits/stdc++.h>

#define endl '\n'

using namespace std;

int solve() {

    /* code */

    return 0;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    // freopen("input.txt", "r", stdin);
    // freopen("output.txt", "w", stdout);

    int tc; cin >> tc; while (tc--) {
        solve();
    }

    return 0;
}